import path from 'path';

export const DOCS_PATH = path.join(process.cwd(), 'bonus/docs/index.html');
export const OPENAPI_SPECS_PATH = path.join(
  process.cwd(),
  'bonus/openapi/specs.openapi.yaml'
);
